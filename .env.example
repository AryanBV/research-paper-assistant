# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration (Choose one)
# Option 1: Connection URL (Production / Railway)
DATABASE_URL=mysql://user:password@localhost:3306/research_paper_assistant

# Option 2: Individual parameters (Development)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=research_paper_assistant

# PDF Generation
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium